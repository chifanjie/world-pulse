# 方法说明

## 1. 时间与范围

- 归档日期使用 `Asia/Shanghai`（UTC+8）。
- 默认观察窗口是发布前 36 小时；重大持续事件可回看更长时间，并明确标注背景日期。
- 主题池包括政治、经济、市场、科技、科学、气候与环境、健康与社会、文化。
- 每日目标是地区和主题多样，而不是固定凑满每个栏目。

## AI 前沿雷达

每期额外选择约 3 条 AI 内容，允许在证据质量需要时保留 2–4 条，通常分别来自模型/产品、前沿论文、以及安全/基础设施/开源三个方向。重大消息观察最近 72 小时；论文默认观察最近 7 天，如果窗口内没有足够高质量结果，可以选更早但仍在快速传播的工作，并注明日期。

候选内容按以下维度排序：

- 原始技术贡献及其潜在影响；
- 是否有论文、模型卡、代码、数据或系统卡可核验；
- 可量化热度信号，例如 Hugging Face Daily Papers 社区票数、GitHub 关注增长、官方发布与多个独立研究团队讨论；
- 与已经收录内容的差异性。

每条必须包含核心技术点、为什么重要、热度依据和局限。公司发布的基准默认视为自报结果；arXiv 论文明确标为预印本，不能把社区票数或社交讨论当作同行评审。

过去 14 天已经收录的同一论文、模型版本或事件不会重复出现，除非有代码发布、正式接收、独立复现或其他实质更新。同一机构原则上每天最多一条。只有至少两个相互独立、带时间戳的关注信号才能称为“最热”或“爆火”；单一平台只能描述为“该平台内热度靠前”。

## 2. 来源优先级

1. 国际组织、政府、央行、统计机构、大学和研究机构等一手来源。
2. Reuters、AP 等有编辑规范的国际通讯社。
3. 信誉良好的区域媒体，用于补足当地视角。

高风险事实尽量满足“一手来源 + 独立可靠来源”或两个相互独立的可靠来源。仅有单一匿名消息源时会降低置信度，并在正文说明。

## 3. 选题与去重

候选事件按照以下维度人工/模型辅助评估：

- 跨区域或跨行业影响
- 对公共政策、经济、科学或社会的长期意义
- 来源可靠性与可验证性
- 当日主题和地区的整体多样性

同一事件的多篇报道会合并为一个条目。不同媒体对同一事实的分歧不会被抹平，而会记录为“不确定性”。

## 4. 置信度

- `high`：有明确一手材料或多个可靠来源相互印证。
- `medium`：主要事实可信，但关键细节仍待确认。
- `low`：信息价值存在，但来源有限或事件快速变化；通常不进入每日主摘要。

## 5. 数据格式

每个 `data/YYYY/MM/YYYY-MM-DD.json` 文件包含：

- `date`、`timezone`、`generated_at`
- 当日总览与条目数组
- 每条事件的类别、地区、摘要、重要性、不确定性、置信度和来源
- `ai_radar.item_ids` 指向当期约 3 条 AI 条目，并可保存论文热度快照
- 来源的发布者、标题、URL、发布日期与来源类型

结构化文件通过 `tools/validate_digest.py` 校验，再由 `tools/build_index.py` 更新全局索引。

## 6. 提交原则

一个提交对应一个可独立审阅的变化，例如日报、周报、小工具、来源规则改进或事实更正。项目不会：

- 创建空提交；
- 修改作者日期来回填历史；
- 用重复内容或无意义格式变化制造提交；
- 将未经核实的传闻写成事实。

每天提交数的差异来自真实工作量：普通日报通常 1 个提交，图表、周报、小工具或纠错会形成额外的原子提交。

### 不规则活动规划

`tools/plan_day.py` 使用当天日期与前 14 个已完成自然日的实际活动生成可复现但不呈周规律的计划；历史包含 0，不包含今天。默认权重为：1 个活动槽 44%、2 个 34%、3 个 17%、4 个 5%。计划器还会打破连续三天相同数量、连续安静期和重复的七天序列。

活动槽只是当天可投入成果的上限，不是必须完成的提交配额。除日报外，每个槽都必须变成独立有用的深度条目、来源审计、时间线、数据视图、方法改进、滚动回顾或经过测试的 lab；没有合适成果就放弃该槽。

滚动回顾在上次回顾后的 5–10 天内选择不固定间隔，lab 在上次成果后的 9–21 天内选择不固定间隔。两者都不绑定星期几。若尚无首个成果，计划器以仓库最早的非合并提交日期 `project_start` 为冷启动锚点；到期成果在真正完成前持续标记为 overdue，不会因跨日重新抽样而消失。

自动化分成两个状态：北京时间 09:00 优先处理日报，13:00 以后继续检查 review 与 lab。日报已经存在只会跳过重复日报，不会跳过项目阶段。计划器给出的泛化候选必须先映射为有明确路径、验收标准和幂等键的具体成果，才能算完成。

### 颜色感知

GitHub GraphQL API 会为每天返回 `contributionCount`、`contributionLevel` 和颜色。等级相对于当前贡献日历计算，因此固定的 1–4 个提交可能全部落在同一浅色档。`tools/github_palette.py` 会在每次规划前读取当前账号的实际分档和已观察到的数量区间，再把这些值交给计划器。

普通内容日只以第一档为目标。只有已经到期且能够完成实现、测试与说明的 review 或 lab，才会把第二档视作愿望上限；大型项目极少考虑第三档，自动化不选择第四档。计划器分别记录 GitHub 已显示的今日账号贡献、本仓库今日已经完成的 `completed_atomic_units`，以及真实、独立、尚未提交的 `planned_atomic_units`，再输出愿望颜色与可达颜色。未显式列出计划单元时，可达结果保持未知；若真实工作不足以跨过动态阈值，可达颜色会自动降级，禁止重复计数、保留互相矛盾的目标、机械拆分或填充提交。

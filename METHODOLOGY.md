# 方法说明

## 1. 时间与范围

- 归档日期使用 `Asia/Shanghai`（UTC+8）。
- 默认观察窗口是发布前 36 小时；重大持续事件可回看更长时间，并明确标注背景日期。
- 主题池包括政治、经济、市场、科技、科学、气候与环境、健康与社会、文化。
- 每日目标是地区和主题多样，而不是固定凑满每个栏目。

## 2. 来源优先级

1. 国际组织、政府、央行、统计机构、大学和研究机构等一手来源。
2. Reuters、AP 等有编辑规范的国际通讯社。
3. 信誉良好的区域媒体，用于补足当地视角。

高风险事实尽量满足“一手来源 + 独立可靠来源”或两个相互独立的可靠来源。仅有单一匿名消息源时会降低置信度，并在正文说明。

## 3. 选题与去重

候选事件按照以下维度人工/模型辅助评估：

- 跨区域或跨行业影响
- 对公共政策、经济、科学或社会的长期意义
- 来源可靠性与可验证性
- 当日主题和地区的整体多样性

同一事件的多篇报道会合并为一个条目。不同媒体对同一事实的分歧不会被抹平，而会记录为“不确定性”。

## 4. 置信度

- `high`：有明确一手材料或多个可靠来源相互印证。
- `medium`：主要事实可信，但关键细节仍待确认。
- `low`：信息价值存在，但来源有限或事件快速变化；通常不进入每日主摘要。

## 5. 数据格式

每个 `data/YYYY/MM/YYYY-MM-DD.json` 文件包含：

- `date`、`timezone`、`generated_at`
- 当日总览与条目数组
- 每条事件的类别、地区、摘要、重要性、不确定性、置信度和来源
- 来源的发布者、标题、URL、发布日期与来源类型

结构化文件通过 `tools/validate_digest.py` 校验，再由 `tools/build_index.py` 更新全局索引。

## 6. 提交原则

一个提交对应一个可独立审阅的变化，例如日报、周报、小工具、来源规则改进或事实更正。项目不会：

- 创建空提交；
- 修改作者日期来回填历史；
- 用重复内容或无意义格式变化制造提交；
- 将未经核实的传闻写成事实。

每天提交数的差异来自真实工作量：普通日报通常 1 个提交，图表、周报、小工具或纠错会形成额外的原子提交。

### 不规则活动规划

`tools/plan_day.py` 使用当天日期与最近 14 天的实际活动生成可复现但不呈周规律的计划。默认权重为：1 个活动槽 44%、2 个 34%、3 个 17%、4 个 5%。计划器还会打破连续三天相同数量、连续安静期和重复的七天序列。

活动槽只是当天可投入成果的上限，不是必须完成的提交配额。除日报外，每个槽都必须变成独立有用的深度条目、来源审计、时间线、数据视图、方法改进、滚动回顾或经过测试的 lab；没有合适成果就放弃该槽。

滚动回顾在上次回顾后的 5–10 天内选择不固定间隔，lab 在上次成果后的 9–21 天内选择不固定间隔。两者都不绑定星期几。

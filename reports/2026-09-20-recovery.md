# 2026-09-20：发布中断恢复记录

本次恢复由维护者在 2026-09-20 明确提出：补上缺期，并在以后出现错误时及时修复和报告。所有补发和 Git 提交都使用实际当前时间，不回填 GitHub 贡献日期。

## 中断证据与原因边界

- 恢复开始时，本地 `main` 和实时远端 `main` 都停在 `1d409816cf38bfc14250c3a0533291567f343f7d`，提交时间为 2026-09-01 11:00:12+08:00；最新一期为 9 月 1 日。
- 此前运行记录多次出现 `schannel: ... SEC_E_NO_CREDENTIALS`，在同步阶段停止。它是 Windows TLS 初始化错误，不能据此断言 GitHub token 过期。
- 旧手册要求网络失败立即停止，但没有针对这一错误的安全恢复路径，也未在每次恢复运行时检查连续缺期。调度触发与发布成功没有形成可验证的闭环。
- 本次首先确认指定路径、唯一 origin、`main`、干净工作树和无进行中的 Git 操作，再执行普通 `pull --ff-only`，成功且无需改动全局配置。随后也验证了启用证书校验的单次 OpenSSL 连接能读取相同远端 SHA。
- 以前受限执行环境与本次环境存在差异，但没有足够证据把它认定为唯一底层原因；目前可证实的是连接已恢复。不会宣称单次成功保证以后网络永不失败。

## 恢复范围与验收单元

本轮范围是最后一次成功发布之后的 **2026-09-02 至 2026-09-20，共 19 期**。8 月原有缺期不属于这次中断的新缺口，也不能因这次补发而宣称整个项目历史无缺期。

只读健康检查还发现 8 月早期缺期 10 天：4、14、15、20、21、22、24、28、29、30 日。它们单独保留为历史缺口，不在本轮 19 期验收中掩盖或计为已完成。

本轮在实施前明确三个独立原子工作单元：

| 单元 / 幂等键 | 文件与验收要求 |
| --- | --- |
| `publication-recovery-2026-09-20` | `tools/safe_sync.py`、发布健康检查 lab、测试和手册：已知 TLS 错误仅单次切换后端；短暂错误有限重试；认证/证书/并发/用户改动停止；push 后核对实时远端 SHA；缺期和真实补发时间可离线检查；全部测试通过。 |
| `backfill-2026-09-02-to-2026-09-20` | 19 对 JSON/Markdown、最新链接及生成索引：来源正文与时间核验、AI 去重、每篇结构校验、没有伪造的生成或提交日期。整批作为一个中断恢复成果，不逐文件拆分提交。 |
| `review-2026-09-11-to-2026-09-20` | 对最近 10 天的 rolling review：引用归档事件 ID、日报及直接来源，区分事实与分析并列出未决问题；不把综述当新事件。 |

历史期以原日期的来源署名日期窗口重建，明确注明实际补发时间。无法确定时区时分的来源不伪装成北京时间 09:00 前已知；当前日报使用实际观察截点。无法验证的候选不因缺期压力纳入。

## 实时规划输入

2026-09-20 规划时，`github_palette.py` 读取到动态目标值 **1 / 12 / 24 / 44**；观察到的区间为 1–11 / 12–19 / 24–33 / 44–81，颜色依次为 `#9be9a8` / `#40c463` / `#30a14e` / `#216e39`。

- `project_start=2026-08-03`；上一 review 结束日期 `2026-08-26`，上一实际 lab 首次进入主线日期 `2026-08-31`。
- 前 14 个已完成自然日（9 月 6–19 日）本仓库非合并提交均为 0。
- 规划开始时 `today_contributions=0`、`completed_atomic_units=0`、经上述验收标准限定的 `planned_atomic_units=3`。
- 计划器输出 `activity_cap=3`，review 自 9 月 2 日到期，lab 于 9 月 20 日到期。
- 愿望上限为 `SECOND_QUARTILE`（12），真实可达为 `FIRST_QUARTILE`（1），预期新增三个独立成果不足以达到第二档；不补空提交、不更改日期、不为颜色继续制造工作。

## 防止再次长期停更

使用 [安全同步工具](../tools/safe_sync.py) 保持普通快进拉取和普通推送，明确开启证书验证，最多重试三次已经失败的连接请求，不对仍在运行的命令设置总时长限制。使用 [发布健康检查](../labs/publication_health/README.md) 区分缺期、坏配对、实际晚发和本地归档；远端发布成功仍必须另核 SHA。

本轮错误要报告类别、修复和最终发布状态。可安全修复的本轮内容/测试错误立即修复并复验；身份、权限、证书、用户改动和并发边界不自动越过。应用离线时不能保证即时执行，下一次恢复运行必须检查实际缺口。

已有 `world-pulse` heartbeat 的执行提示已更新，保留原名称、启用状态、当前聊天以及每天 09:00–23:00 每小时运行的时间表，没有新建重复自动化。根据 [OpenAI 官方计划任务说明](https://learn.chatgpt.com/docs/automations?surface=app)，本地文件任务仍需电脑开机且应用运行；本次不改权限配置或假定离线也能执行。

## 验收记录

新增同步与健康检查测试覆盖 TLS 后端切换、实际生效的证书验证、重试上限、认证错误不绕过、工作树/分支/origin、并发远端、push 后 SHA、重复 push 幂等、跨月缺期、晚发、时区、未来生成时间和旧日 Markdown 错配。独立审阅发现错配假阳性后，已先修复再加反例测试。运行 `python -m unittest discover -s tests -v` 已通过 42 项测试。

日报批次本地验收已完成：19 期、110 条世界事件、38 条 AI，共 148 条内容；185 条来源引用、184 个不同 URL。URL 数不是独立核验方数量。9 月 9、12、13 日分别保留 5、4、5 条世界事件，并说明缺口；不为达到条数补入低实质性会议，也不把次日北京时间才确定的比赛、颁奖结果提前归档。

- 全仓 39 份日报 JSON 及 Markdown 配对均通过 `tools/validate_digest.py`。
- 9 月 2–20 日健康检查为 `complete`，19/19 期、无缺期、无坏配对；2–19 日正确识别为晚发，20 日为当天稿。
- `data/index.json` 与来源多样性数据已重新生成；README 最新一期指向 9 月 20 日。
- 新增 AI 条目已对最近 14 天去重；唯一跨日重复的来源 URL 为 Google 更新日志中的两个不同日期、不同产品更新，并非重复新闻。明确提供时区的来源已检查没有越过对应归档日的北京时间边界。
- [9 月 11–20 日滚动回顾](../weekly/2026/09/2026-09-11-to-2026-09-20.md)覆盖最近十期的全部 77 个事件 ID（57 条世界事件、20 条 AI），汇总 90 次来源引用、89 个唯一 URL，保留历史补发、单一来源、原文访问和独立复现等限制；未增补未经日报核验的新事实。
- 本记录写于提交与推送验收过程中，本地通过不自行证明 GitHub 发布成功；最终结果以 `safe_sync.py` 的实时远端 SHA 核对及本轮完成通知为准。
